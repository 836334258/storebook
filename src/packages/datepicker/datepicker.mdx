import { Canvas, Meta, ArgTypes } from '@storybook/blocks';

import * as Demo from './Datepicker.stories';

<Meta of={Demo} />

# DatePicker 日期选择器

时间选择器，支持日期、年月、时分等维度，通常与弹出层组件配合使用。

## Props

<ArgTypes of={Demo} />